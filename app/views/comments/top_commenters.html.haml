%h1 Top 10 commenters this week
%table.table.table-striped.table-hover
  %thead
    %tr
      %th #
      %th Name
  %tbody
  - @top_commenters.each_with_index do |person, index|
    -if person
      %tr
        %td= index+1
        %td= person.name
